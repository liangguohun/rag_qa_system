version: '3.8'

services:
  rag-api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./vector_stores:/app/vector_stores
    environment:
      - USE_LOCAL_LLM=true
    restart: unless-stopped